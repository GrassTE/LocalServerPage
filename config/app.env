# LocalServerPage runtime config.
# Docker can mount this whole config directory and edit these values without rebuilding.

PORT=3000
STATUS_TIMEOUT_MS=3000

# Comma-separated hostnames that should be treated as internal access.
# Example: INTERNAL_HOSTS=home.example.com,server.lan
INTERNAL_HOSTS=
